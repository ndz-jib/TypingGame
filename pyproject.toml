[project]
name = "GameTyping"
version = "0.1.0"
description = "Nothing here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flash>=1.0.3",
    "flask>=3.1.3",
    "flask-cors>=6.0.2",
]
